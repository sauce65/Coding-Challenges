#This finds the sum of all integers with factors of x or y BELOW value n 

def mult(n, x, y):
    i = 0
    total = 0
    while i < n:
        if i % x == 0 or i % y == 0:
            total += i
            i += 1
        else:
            i += 1
            continue
    print(total)  
