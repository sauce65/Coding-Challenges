#checks if an integer is divisible by a list of primes

def p_fact(y, lst):
	factor_count = 0
	for i in lst:
		if y % i == 0:
		factor_count += 1
		break
	else:
		pass
	return factor_count    

#iterates up to n and adds new found primes to a list, checking for primality via p_fact

def n_gen(n, lst):
	for y range(2, n):
		has_fact = p_fact(y, lst)
		if has_fact == 0:
			lst.append(y)
			print(y)
		else:
			pass

#LAST FUNCTION LOST, will figure out later!
